package com.example.notesprojectapp.viewmodel

import com.example.domain.model.HomeNotesModel
import com.example.domain.model.model

data class TempState(

    val tempNotes: List<model> = emptyList(),
)
